#ifndef CONSTANTS_H
#define CONSTANTS_H

const int SCREEN_WIDTH = 1000;// Size of the window that is displayed:
const int SCREEN_HEIGHT = 600;

const int ROW=70; // Row in the grid.
const int MAX=60; // COlumn in the grid.
const int POP=200;// Number of cells to randomly populate.

const int FRAME=5;
const int CELLWIDTH=5;//cell width
const int CELLLENGTH=5;//cell length
const int SPACE=5;// Space between each cell in the grid.

const int GAP=25; // Gap between the option buttons on the window.
const int OGY=10;// the original y posiion for the option boxes.
const int VecX =110;// The width of each option box.
const int VecY=60;// The length of each option box.

const int XMIN=800;//The x Position where the option boxes begin on the window.

const int XSTART=0;// Start of x position in the grid.
const int YSTART=0;//start of the y position in the grid.


#endif // CONSTANTS_H
